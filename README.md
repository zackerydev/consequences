# consequences

Demo repo for understanding GitHub Actions!

Local Setup

### Prerequisites

- Install `asdf`

```bash
asdf plugin add nodejs
asdf plugin add pnpm
asdf install
pnpm i
pnpm -r typecheck
pnpm -r test
pnpm lint
pnpm check
pnpm format
```
